/**
 * Fungsi: Koleksi skema validasi Zod untuk data kritis aplikasi
 * Digunakan oleh: Form komponen, API routes
 * Tujuan: Single source of truth untuk validasi data di frontend
 */

import { z } from "zod"